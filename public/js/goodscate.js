myapp.controller('myCtrl', ['$scope', '$http', '$compile', 'retrieve', function ($scope, $http, $compile, retrieve) {



}]);

function togglecate(id,type,token) {
    alert(id+'--'+type+'--'+token)
    // var fd = new FormData();
    // fd.append('id', id);
    // fd.append('deleted', type);
    // fd.append('token', token)
    // $.post('/backend/xuduobal/goodscategory',fd,function (res) {
    //     console.log(res)
    // })

}
